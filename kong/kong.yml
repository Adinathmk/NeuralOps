# ============================================================
# NeuralOps — Kong Declarative Configuration (DB-less mode)
# ============================================================
# Kong applies rate limiting and routes:
#   /api/v1/*, /ws/*, /health  → FastAPI (port 8001)
#   everything else            → Django  (port 8000)
#
# JWT validation is applied specifically to protected routes.
# ============================================================

_format_version: "3.0"
_transform: true

# ── Upstreams ──────────────────────────────────────────────────────────────────
upstreams:
  - name: django-upstream
    targets:
      - target: django:8000
        weight: 100

  - name: fastapi-upstream
    targets:
      - target: fastapi:8001
        weight: 100

# ── Services ───────────────────────────────────────────────────────────────────
services:
  # ── Django Core Management Service ─────────────────────────────────────────
  - name: django-service
    host: django-upstream
    port: 80
    protocol: http
    connect_timeout: 60000
    read_timeout: 120000
    write_timeout: 120000
    routes:
      - name: auth-routes
        paths:
          - /api/auth
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        # PUBLIC: No JWT validation required.

      - name: admin-routes
        paths:
          - /api/admin
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        plugins:
          - name: jwt
            config:
              claims_to_verify: [exp]
              key_claim_name: iss
          - name: request-transformer
            config:
              add:
                headers: ["X-Gateway-Validated: true"]

      - name: tenants-routes
        paths:
          - /api/tenants
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        plugins:
          - name: jwt
            config:
              claims_to_verify: [exp]
              key_claim_name: iss
          - name: request-transformer
            config:
              add:
                headers: ["X-Gateway-Validated: true"]

      - name: users-routes
        paths:
          - /api/users
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        plugins:
          - name: jwt
            config:
              claims_to_verify: [exp]
              key_claim_name: iss
          - name: request-transformer
            config:
              add:
                headers: ["X-Gateway-Validated: true"]

      - name: billing-routes
        paths:
          - /api/billing
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        plugins:
          - name: jwt
            config:
              claims_to_verify: [exp]
              key_claim_name: iss
          - name: request-transformer
            config:
              add:
                headers: ["X-Gateway-Validated: true"]

      - name: collaboration-routes
        paths:
          - /api/collaboration
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        plugins:
          - name: jwt
            config:
              claims_to_verify: [exp]
              key_claim_name: iss
          - name: request-transformer
            config:
              add:
                headers: ["X-Gateway-Validated: true"]

      - name: static-routes
        paths:
          - /static
        strip_path: false
        preserve_host: true
        protocols: [http, https]

      - name: media-routes
        paths:
          - /media
        strip_path: false
        preserve_host: true
        protocols: [http, https]

      # Django admin panel (Swagger, DRF browsable API, Django admin)
      - name: django-admin-panel
        paths:
          - /admin
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        # PUBLIC/Session Auth

  # ── FastAPI AI + Real-time Service ─────────────────────────────────────────
  - name: fastapi-service
    host: fastapi-upstream
    port: 80
    protocol: http
    connect_timeout: 60000
    read_timeout: 120000
    write_timeout: 120000
    routes:
      - name: api-v1-routes
        paths:
          - /api/v1
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        plugins:
          - name: jwt
            config:
              claims_to_verify: [exp]
              key_claim_name: iss
          - name: request-transformer
            config:
              add:
                headers: ["X-Gateway-Validated: true"]

      - name: websocket-routes
        paths:
          - /ws
        strip_path: false
        preserve_host: true
        protocols: [http, https]
        headers:
          Upgrade:
            - websocket
        plugins:
          - name: jwt
            config:
              claims_to_verify: [exp]
              key_claim_name: iss
          - name: request-transformer
            config:
              add:
                headers: ["X-Gateway-Validated: true"]

      - name: health-route
        paths:
          - /health
        strip_path: false
        preserve_host: true
        protocols: [http, https]

# ── Consumers ──────────────────────────────────────────────────────────────────
consumers:
  - username: neuralops-app
    jwt_secrets:
      - key: neuralops-jwt-key          # matches the "iss" claim in Django-issued JWTs
        algorithm: RS256
        rsa_public_key: |
          -----BEGIN PUBLIC KEY-----
          
          QQIDAQAB
          -----END PUBLIC KEY-----

# ── Global Plugins ─────────────────────────────────────────────────────────────
plugins:
  # ── Rate Limiting ─────────────────────────────────────────────────────────
  - name: rate-limiting
    config:
      minute: 60
      policy: local
      hide_client_headers: false
      fault_tolerant: true

  # ── CORS (Cross-Origin Resource Sharing) ──────────────────────────────────
  - name: cors
    config:
      origins:
        - "http://localhost:3000"
        - "http://localhost:5173"
      methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      headers:
        - Accept
        - Accept-Version
        - Content-Length
        - Content-MD5
        - Content-Type
        - Date
        - Authorization
      exposed_headers:
        - X-Auth-Token
      credentials: true
      max_age: 3600

  # ── JWT Claims Injection (FastAPI Strategy 1) ─────────────────────────────
  - name: pre-function
    config:
      access:
        - |
          local auth_header = kong.request.get_header("Authorization")
          if auth_header and string.sub(auth_header, 1, 7) == "Bearer " then
            local token = string.sub(auth_header, 8)
            local payload_b64 = token:match("^[^%.]+%.([^%.]+)%.[^%.]+$")
            if payload_b64 then
              local remainder = #payload_b64 % 4
              if remainder > 0 then
                payload_b64 = payload_b64 .. string.rep("=", 4 - remainder)
              end
              local payload_b64_url = payload_b64:gsub("-", "+"):gsub("_", "/")
              local decoded = ngx.decode_base64(payload_b64_url)
              if decoded then
                local cjson = require("cjson.safe")
                local payload = cjson.decode(decoded)
                if payload then
                  if payload.tenant_id then kong.service.request.set_header("x-tenant-id", payload.tenant_id) end
                  if payload.user_id then kong.service.request.set_header("x-user-id", payload.user_id) end
                  if payload.role then kong.service.request.set_header("x-user-role", payload.role) end
                end
              end
            end
          end
