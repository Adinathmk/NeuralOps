[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts = --cov=app --cov-report=html --cov-report=term-missing --strict-markers
filterwarnings =
    ignore::DeprecationWarning

